задача 5 можна в вал покласти функці і не потріно бескінечний цикл
            while (true) {
                if (!isValidWindow(lettersCountInWindow)) {
                    break;
                }
while(isValidWindow(lettersCountInWindow))

задача 7 говорить що складність Он але два рази ходимо по списку

задача 11 потрібно використати LinkedHashMap для порядку якщо це буде потрібно

задача 13 потрібно повторити